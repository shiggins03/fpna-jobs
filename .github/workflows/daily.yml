name: daily
on:
  schedule:
    - cron: "30 12 * * *"  # daily ~8:30am Eastern.
    # Deliberately staggered off the sibling unifier-jobs board (11:00 UTC):
    # the two rosters share several employers (Amazon, Meta, NVIDIA, Oracle,
    # Accenture), so the runs should not hit the same hosts at the same minute.
  workflow_dispatch: {}   # manual trigger button
permissions:
  contents: write
concurrency:
  group: daily
  cancel-in-progress: false
jobs:
  scrape:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install -r requirements.txt
      - name: run pipeline
        run: python -m scraper.main
        env:
          JSEARCH_API_KEY: ${{ secrets.JSEARCH_API_KEY }}
          ADZUNA_APP_ID: ${{ secrets.ADZUNA_APP_ID }}
          ADZUNA_APP_KEY: ${{ secrets.ADZUNA_APP_KEY }}
          JOOBLE_API_KEY: ${{ secrets.JOOBLE_API_KEY }}
      - name: commit results
        run: |
          git config user.name "fpna-jobs-bot"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data docs
          git diff --cached --quiet || git commit -m "daily run $(date -u +%F)"
          git push
